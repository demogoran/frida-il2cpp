{
  "name": "frida-project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "frida": "^16.0.1",
    "frida-il2cpp-bridge": "^0.7.13",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "tsc-version": "tsc --version",
    "start": "rimraf dist && tsc && frida-compile ./dist/script.js -o ./dist/run.js && node main",
    "start-sss": "cd sssurvivers && npm run start"
  },
  "devDependencies": {
    "@types/frida-gum": "*",
    "@types/node": "*",
    "frida-compile": "^10.2.0"
  }
}
